const showsContainer = document.querySelector('.main_content_container');
const searchForm = document.querySelector('form');
const searchFormInput = document.getElementById('search_text');
const selectShows = document.getElementsByClassName('show_container');
const displayAllCounter = document.getElementById('all_elements_counter');

export {
  showsContainer, searchForm, searchFormInput, selectShows, displayAllCounter,
};
